{% extends "base.html.twig" %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('build/add-lesson.css')}}"> 
{% endblock %}
{% block javascripts %}
    <script src="https://kit.fontawesome.com/640b361ca9.js" crossorigin="anonymous"></script>
{% endblock %}
{% block body %}
<div class="form-container">
{{form_start(form)}}
	<h3>ajouter lesson </h3>
    <div class="form-group">
		{{ form_label(form.fileName)}}
		{{form_widget(form.fileName)}}
	</div>
    <div class="form-group">
		{{ form_label(form.categorie)}}
		{{form_widget(form.categorie)}}
	</div>
<button type="submit">ajouter</button>
<a href="{{path('app_lessons_all')}}" > retourner a la liste des lessons </a>
{{form_end(form)}}
<div>  
{% endblock %}